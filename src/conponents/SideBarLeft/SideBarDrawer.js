import React, { Component } from 'react';
import {SideBarLeft} from "../Home/Router";

export default class SideBarDrawer extends Component {
    render() {
        return (
            <SideBarLeft/>
        );
    }
}