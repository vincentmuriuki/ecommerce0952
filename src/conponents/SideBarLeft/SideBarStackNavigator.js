import React, { Component } from 'react';
import {SideBarStack} from "../Home/Router";

export default class SideBarStackNavigator extends Component {
    render() {
        return (
            <SideBarStack/>
        );
    }
}